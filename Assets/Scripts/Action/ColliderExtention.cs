using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColliderExtention : MonoBehaviour
{
    /*public List<ActionBaseComponent> insideObjects;

    private void OnTriggerEnter(Collider other)
    {
        Debug.Log(other.name);
        if(other.GetComponent<TurretAim>())
        insideObjects.Add(other.GetComponent<TurretAim>());
    }

    private void OnTriggerExit(Collider other)
    {

        if (other.GetComponent<TurretAim>())
            insideObjects.Remove(other.GetComponent<TurretAim>());
    }*/

}
